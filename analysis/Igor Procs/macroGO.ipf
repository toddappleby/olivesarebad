#pragma TextEncoding = "UTF-8"
#pragma rtGlobals=3		// Use modern global access method and strict wave access.

macro doObar()

makeOBarPolar()
monkeyBusiness(::::polarity)
ohGod()

end

macro doMbar()

mbCreate()
mbAnnotate(::::polarity)
mbSubs(::::regionOut)

end